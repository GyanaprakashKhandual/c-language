// Instruction in C


/*
1. Type Declaration:
   - Declare variables with appropriate types.
   - Use `int`, `float`, `double`, and `char` as needed.
2. Arithimetic Operations:
   - Perform addition, subtraction, multiplication, and division.
   - Use operators like `+`, `-`, `*`, `/`.
3. Control Structures:
   - Implement `if`, `else if`, and `else` statements.
   - Use `for`, `while`, and `do-while` loops.
*/

/*

#include<stdio.h>

int main () {
    // Variable Declaration
    int a = 10, b = 20;
    float c = 5.5, d = 2.2;
    char e = 'A';

    // Arithmetic Operations
    int sum = a + b;
    float division = c / d;

    // Control Structures
    if (sum > 20) {
        printf("Sum is greater than 20\n");
    } else {
        printf("Sum is less than or equal to 20\n");
    }

    for (int i = 0; i < 5; i++) {
        printf("Loop iteration: %d\n", i);
    }

    return 0;
}
*/

// Practice Code Snippet
/*

#include<stdio.h>

int main () {
    int a = 5 * 2 - 3 + 8 / 4;
    printf("The result of the expression is: %d\n", a);
    return 0;
}
*/

// Relational Operators

/*
// Equality opreators
#include<stdio.h>
int main () {
    printf("Relational Operators: %d\n", 4 == 4);
    return 0;
}
*/

// Greater than and Less than operators

/*
#include<stdio.h>
int main () {
    printf("Greater than and Less than Operators: %d\n", 5 > 3);
    printf("Greater than and Less than Operators: %d\n", 2 < 4);
    return 0;
}
*/

// Greater than or equal to and Less than or equal to operators
/*
#include<stdio.h>
int main () {
    printf("Greater than or equal to and Less than or equal to Operators: %d\n", 5 >= 5);
    printf("Greater than or equal to and Less than or equal to Operators: %d\n", 3 <= 4);
    return 0;
}
*/

// Not equal to operator

/*
#include<stdio.h>
int main () {
    printf("Not equal to Operator: %d\n", 5 != 3);
    return 0;
*/

// Logical Operators

/*
#include<stdio.h>
int main () {
    // Logical AND operator
    printf("Logical AND Operator: %d\n", (5 > 3) && (4 < 6));
    
    // Logical OR operator
    printf("Logical OR Operator: %d\n", (5 > 3) || (4 > 6));
    
    // Logical NOT operator
    printf("Logical NOT Operator: %d\n", !(5 > 3));
    
    return 0;
}
*/


// Assignment Operators 

/*
#include<stdio.h>
int main () {
    int a = 5; // Assignment operator
    printf("Assignment Operator: %d\n", a);
    
    a += 3; // Add and assign
    printf("Add and Assign Operator: %d\n", a);
    
    a -= 2; // Subtract and assign
    printf("Subtract and Assign Operator: %d\n", a);
    
    a *= 2; // Multiply and assign
    printf("Multiply and Assign Operator: %d\n", a);
    
    a /= 4; // Divide and assign
    printf("Divide and Assign Operator: %d\n", a);
    
    return 0;
}
*/